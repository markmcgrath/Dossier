# Execution Prompt for Plan 13 — Quality Audit Remediation

**Model:** Claude Sonnet
**Context:** This prompt is self-contained. The executing model has no prior conversation history.

---

## Prompt

```
You are implementing a remediation plan for the Dossier job search vault. The plan is at `features/plan/13-quality-audit-remediation.md` — read it first for full context. It has four streams (A–D) with 19 tasks total. Execute Streams A, B, and C in order. Stream D tasks D.1–D.3 should also be executed; D.4 and D.5 are deferred (skip them).

**Before starting:** Read these files to understand current state:
- `CLAUDE.md` (project rules — especially the open-source propagation rules)
- `features/plan/13-quality-audit-remediation.md` (the full plan)
- `skill-update/SKILL.md` (the skill definition you'll be editing)
- `skill-update/references/mode1-offer-evaluator.md` (eval template — primary edit target)
- `skill-update/references/scoring-guide.md` (scoring calibration)
- `skill-update/references/mode9-inbox-followup.md` (recruiter triage)
- `skill-update/references/weekly-trend-report.md` (weekly trends)
- `skill-update/references/mode2-portal-scan.md` (daily scan)
- `README.md` (frontmatter conventions section)

Use a task list to track progress through the plan.

---

## Stream A — Eval Schema & Template Parity

### A.1 — Add 10-Dimension Scoring Table to Eval Template

Edit `skill-update/references/mode1-offer-evaluator.md`. In the output template code block, insert a new section AFTER the `## CV Tailoring Suggestions` section and BEFORE the `*This evaluation was generated by an AI model...` disclaimer line. Add:

```
## 10-Dimension Scores

| # | Dimension | Score | Weight | Contribution |
|---|-----------|-------|--------|--------------|
| 1 | Role & Responsibility Match | X.X | 20% | X.XX |
| 2 | Skills & Experience Alignment | X.X | 20% | X.XX |
| 3 | Seniority & Level Fit | X.X | 10% | X.XX |
| 4 | Compensation vs. Market | X.X | 10% | X.XX |
| 5 | Growth & Advancement Potential | X.X | 10% | X.XX |
| 6 | Company Strength & Stability | X.X | 10% | X.XX |
| 7 | Culture & Values Signals | X.X | 8% | X.XX |
| 8 | Remote / Location Fit | X.X | 5% | X.XX |
| 9 | Hiring Signal Quality | X.X | 4% | X.XX |
| 10 | Strategic Career Value | X.X | 3% | X.XX |
| | **Weighted Total** | | | **X.XX** |

*If any dimension score deviates more than 1 point from what the weighted average implies, add a one-line annotation explaining why.*
```

### A.2 — Add Bias Caveat Section

Same file, same output template code block. Insert AFTER the 10-dimension table (A.1) and BEFORE the AI disclaimer:

```
## Bias Caveat

This evaluation does not account for:
- Actual manager/team quality (unknown without insider information)
- Internal org dynamics, recent reorgs, or undisclosed headcount changes
- Personal energy or enthusiasm for this role vs. others in the pipeline
- Anchoring effects from previously evaluated roles

If this role is from a prestigious company, check: are dimensions 6–7 inflated by brand recognition?
If this role scores lower than a previous favorite, check: is the comparison anchored on that earlier eval?
```

### A.3 — Add Career-Page Cross-Check to Legitimacy

Same file, in the "Signals to assess" numbered list (currently items 1–5). Add item 6 after item 5:

```
6. **Career page verification** — Does the posting appear on the company's own careers page? Listings that appear only on aggregators (Indeed, LinkedIn, Dice) but not on the company's website are a significant ghost job indicator. If browser tools are available, check the company careers page directly.
```

### A.4 — Expand Eval Frontmatter Schema

**File 1:** `skill-update/SKILL.md`. Find the frontmatter schema code block (starts around line 46 with `type: eval`). Add these three fields AFTER the `sources: []` line and BEFORE the closing ``` :

```yaml
source: "Indeed" | "Dice" | "LinkedIn" | "Company Careers" | "Referral" | "Recruiter Inbound" | "Other"
referral_contact: ""  # Name of internal champion or referring person, if any
application_method: ""  # Cold | Referral | Recruiter Inbound | Networking — populated when the user applies
```

**File 2:** `skill-update/references/mode1-offer-evaluator.md`. After the existing post-eval steps (the numbered list that starts with "After delivering the report:"), add a note:

```
**New frontmatter fields:** `source` should be auto-populated from the JD origin (Indeed, Dice, LinkedIn, etc.). `referral_contact` should be filled if the user mentions a referral or networking contact. `application_method` stays empty until the user actually applies.
```

**File 3:** `README.md`. Find the eval frontmatter code block (starts around line 142). Add these three fields after the `notes:` line, before the closing `---`:

```yaml
source: "Indeed" | "Dice" | "LinkedIn" | "Company Careers" | "Referral" | "Recruiter Inbound" | "Other"
referral_contact: ""
application_method: ""
```

### A.5 — Backfill April 14 Eval Frontmatter

Scan `evals/` for all `.md` files. For each file, read its frontmatter and check for missing fields. Add any missing fields with these defaults:

- `outcome: Pending` (if missing)
- `legitimacy: Plausible` (if missing — unless the eval body mentions legitimacy concerns, in which case use the appropriate tier)
- `model: ""` (if missing)
- `sources: []` (if missing)
- `source: ""` (if missing — try to infer from eval body text or daily scan references; use "Indeed" or "Dice" if the eval mentions those sources)
- `referral_contact: ""` (if missing)
- `application_method: ""` (if missing)

**Special fix:** In `evals/eval-ukg-2026-04-14.md`, find the body heading that says "Grade: A-" and change it to "Grade: A" to match the frontmatter value `grade: A`.

Do NOT change any existing field values — only add missing fields. Preserve the exact formatting and content of each file.

### A.6 — Add Grade Validation Rule

In `skill-update/references/mode1-offer-evaluator.md`, find the numbered post-eval steps (the list starting with "After delivering the report:"). The current step 2 is the dedup check. Renumber existing step 3 onwards and insert a new step 3:

```
3. **Grade consistency check:** Verify the `grade:` value in frontmatter matches the grade in the body heading (`**Grade: [LETTER] | ...**`). If they differ, reconcile to the computed grade from the 10-dimension scoring table.
```

---

## Stream B — Outreach & Follow-up

### B.1 — Create Outreach Reference File

Create a NEW file at `skill-update/references/mode5-outreach.md` with this exact content:

```markdown
# Mode 5: Outreach — Reference

*This file is loaded by the dossier skill on demand. Do not edit without also updating the pointer in SKILL.md.*

**Trigger:** User asks to draft outreach for a specific role, or the eval post-action menu (Mode 1 step 6) leads here.

## Frontmatter Schema

```yaml
type: outreach
company: "Company Name"
role: "Job Title"
channel: "LinkedIn" | "Email" | "LinkedIn + Email"
date: YYYY-MM-DD
status: drafted | sent | replied | no-response | archived
related_eval: "[[eval-company-slug-date]]"
target_titles: ""  # comma-separated list of target recipient titles
sent_date: ""
first_response_date: ""
followup_dates: []
touch_count: 0
```

## Output Template

```
# [Company] Outreach — [Role Title]

## Email Version

**Subject:** [Role title] — quick note

[Body: 150–250 words. Professional tone. Lead with the strategic angle from the eval. Reference a specific company initiative, not generic praise. Close with a clear ask (15-min call).]

---

## LinkedIn Connection-Request Version (<300 chars)

[Short, conversational. Reference the role and one specific credential.]

---

## Follow-up Schedule

| Touch | Channel | Date | Status |
|-------|---------|------|--------|
| 1 — Initial | [LinkedIn/Email] | [send date] | Pending |
| 2 — Follow-up | [Email/LinkedIn] | [send date + 5–7 days] | Pending |
| 3 — Final | [Email] | [Touch 2 + 7–10 days] | Pending |
| Archive | — | [Touch 3 + 14 days] | — |

After 3 touches with no response, archive the outreach. Do not follow up a 4th time.

---

## Notes Before Sending

- [Strategic framing: which narrative to lead with and why]
- [Channel sequencing: LinkedIn first → email follow-up, or vice versa]
- [Any gates or conditions from the eval that must be resolved before sending]
```

## Channel Guidelines

- **LinkedIn connection requests:** Max 300 characters. Conversational. Reference one specific credential + the role.
- **LinkedIn InMails / messages:** Under 150 words. Personalize beyond "I saw your profile" — reference a company project, blog post, or initiative.
- **Email:** 150–300 words. Professional. Quantified proof points. Clear subject line with role title.
- **Multi-channel:** Send LinkedIn first. If no response in 5–7 days, follow up via email. Never send both on the same day.

## Follow-up Cadence Rules

- Touch 1 (initial outreach): day 0
- Touch 2 (follow-up): day 5–7
- Touch 3 (final): day 12–17
- Archive (no further contact): day 26–31
- When updating frontmatter after sending, set `sent_date` to today, increment `touch_count`, and append the next follow-up date to `followup_dates`.
```

Then edit `skill-update/SKILL.md`. Find the Mode 5 section (### Mode 5: Outreach, around line 234). Add this line immediately after the section header and before the **Trigger:** line:

```
Read `references/mode5-outreach.md` for the output template, frontmatter schema, follow-up cadence, and channel guidelines.
```

### B.2/B.3 — Backfill Existing Outreach Docs

Edit `outreach/outreach-ukg-2026-04-14.md`. Add these fields to its YAML frontmatter (after the existing fields, before the closing `---`):

```yaml
sent_date: ""
first_response_date: ""
followup_dates: []
touch_count: 0
```

Also add a follow-up schedule skeleton to the body, before the "## Notes Before Sending" section:

```markdown
## Follow-up Schedule

| Touch | Channel | Date | Status |
|-------|---------|------|--------|
| 1 — Initial | LinkedIn | TBD | Pending |
| 2 — Follow-up | Email | TBD | Pending |
| 3 — Final | Email | TBD | Pending |
| Archive | — | TBD | — |
```

Do the same for `outreach/outreach-guy-carpenter-2026-04-14.md`.

---

## Stream C — Daily & Weekly Report Enhancements

### C.1 — Add Source Yield Tracking to Daily Scans

Edit `skill-update/references/mode2-portal-scan.md`. Find the output template section (the code block starting with `# Portal Scan — [YYYY-MM-DD]`). Add a new section BEFORE the `## Next Steps` section:

```markdown
## Source Yield

| Source | Roles Logged | Avg Grade | Best Lead |
|--------|-------------|-----------|-----------|
| [Source 1] | X | [grade] | [Company — Role] |
| [Source 2] | X | [grade] | [Company — Role] |

*Populated from the current scan. Weekly trend report aggregates across scans.*
```

### C.2 — Add Conditional Recruiter Triage Template

Edit `skill-update/references/mode9-inbox-followup.md`. Add a new section at the END of the file (after the "Operating principles" section):

```markdown
---

## Conditional: Zero Inbound for 2+ Weeks

If recruiter triage finds zero inbound for two or more consecutive runs, replace the standard output with an outbound-activation format:

### Outbound Activation Check

- Scan `outreach/` for files with `status: drafted` (unsent). List each with company, role, and date drafted.
- Scan `evals/` for files with `grade` of B or higher and `status: Evaluating` (not yet applied). List the top 3 by score as "highest-leverage outbound moves."
- If unsent outreach drafts exist, surface them first — these are the lowest-friction actions available.

### Profile Visibility Check

- When was the LinkedIn profile last updated? (Ask the user if unknown.)
- Are the headline and summary aligned with `profile.md` target roles?
- Is the "Open to Work" signal active on LinkedIn?

### Search Aperture Check

- Are there job boards not yet being scanned? (Wellfound, Otta, Built In, company career pages)
- Should the keyword set be expanded? (Review `profile.md` match signals vs. current search terms)
- Has the geographic or remote filter changed since the search started?

**When to trigger:** Check the dates of the two most recent `daily/recruiter-triage-*.md` files. If both report zero actionable inbound, switch to this format.
```

### C.3 — Add Conversion Funnel and Trend Metrics to Weekly Digest

Edit `skill-update/references/weekly-trend-report.md`. Add two new sections BEFORE the existing `**Interpretation tips:**` section at the end of the output template:

```markdown
## Conversion Funnel

| Stage | Count | Rate | Δ vs Last Week |
|-------|-------|------|----------------|
| Roles Scanned | X | — | +/- X |
| Evaluating | X | X% of scanned | +/- X |
| Applied | X | X% of evaluating | +/- X |
| Interviewing | X | X% of applied | +/- X |
| Offer | X | X% of interviewing | +/- X |

*Funnel rates are cumulative. Early weeks will show zeros in later stages — that's expected.*

## Compensation Summary

| Metric | This Week | Last Week | Trend |
|--------|-----------|-----------|-------|
| Median (pipeline) | $X | $X | ↑/↓/→ |
| Range | $X–$Y | $X–$Y | — |
| % above comp floor | X% | X% | — |

*If compensation has been trending downward for 3+ weeks, flag: "Your comp expectations may be anchoring low — review profile.md comp floor and consider whether recent scans are skewing your baseline."*
```

Also add week-over-week delta notation guidance to the "Volume trend" section (item 1 in the "If 4+ weeks exist:" list). After the existing bullet "Week-over-week growth or decline (% change)", add:

```
- When reporting pipeline counts in any output, always include the delta: e.g., "Evaluating: 22 (+12 vs last week)"
```

### C.4 — Add Burnout/Pace Monitoring to Daily Digest

Edit the scheduled prompt at `schedule prompts/Daily Digest.md`. In the template section (the code block that defines the digest output format), add a new section AFTER `## Top Priority` and BEFORE the end of the template:

```
## Pace Check (rolling 7 days)

Roles scanned: [count from daily-scan files in last 7 days] | Evals written: [count from evals/ dated in last 7 days] | Applications submitted: [count of evals with status: Applied and date in last 7 days]

[If evals written > 10 on any single day in the window, add:]
⚠️ High pace detected on [date] ([N] evals). Research suggests evaluation quality degrades above ~10 targeted evals per day. Consider batching scan-only days to maintain quality.
```

### C.5 — Enhance Week-Ahead Document

Edit the scheduled prompt at `schedule prompts/Sunday Prep.md`. In the output template (the code block), add three new sections AFTER `## Recommended Focus` and BEFORE the end of the template:

```
## Aging Alerts

[Scan evals/ for files where status is Evaluating and date is 14+ days ago. These listings may have gone stale.]
[For each: **[Company]** — [Role] | Evaluated [date] | [N] days old — consider applying or passing]
[If none: "No aging evals."]

## Decision Gates

[Scan evals/ for files where notes mention unresolved conditions: remote policy, end-client disclosure, rate negotiation, etc.]
[For each: **[Company]** — Gate: [description] — action needed to unblock]
[If none: "No open gates."]

## Outreach Follow-ups Due This Week

[Scan outreach/ for files where sent_date is populated and the next date in followup_dates falls within the coming 7 days.]
[For each: **[Company]** — Touch [N] due [date] via [channel]]
[If none: "No follow-ups due."]
```

---

## Stream D — Interview Prep & Scoring (D.1–D.3 only)

### D.1 — Add Competency Mapping Matrix to Interview Prep

Edit `skill-update/SKILL.md`. Find Mode 3 (### Mode 3: Interview Prep, around line 192). Find the line that starts with "Save the prep document as" (around line 211). Insert this BEFORE that save instruction:

```markdown
**Competency coverage matrix:** After drafting the prep doc, cross-reference `stories.md` against the target JD's key competencies:

| Competency (from JD) | Story Available? | Story Title | Gap? |
|-----------------------|-----------------|-------------|------|
| [e.g., Cross-functional leadership] | ✓ / ✗ | [story name from stories.md] | [If ✗: "Suggest adding a story for this competency"] |

If a critical JD competency has no matching story in `stories.md`, flag it explicitly and suggest the user draft one before the interview.
```

### D.2 — Note Alternative Interview Frameworks

Same file, same Mode 3 section. Insert this AFTER the competency matrix addition (D.1) and BEFORE the save instruction:

```markdown
**Framework selection:** Default to STAR+R (the format used in `stories.md`). For senior/leadership interviews, consider SHARE (Situation, Hindrance, Action, Results, Evaluation). For roles emphasizing learning and growth, consider CARL (Context, Action, Result, Learning). Note the chosen framework in the prep doc header.
```

### D.3 — Expand Flexibility Signals in Scoring Guide

Edit `skill-update/references/scoring-guide.md`. Find the Dimension 8 section (### 8. Remote / Location Fit (5%)). Replace the entire section with:

```markdown
### 8. Remote / Location & Flexibility Fit (5%)
Does the work location, remote policy, AND work-style flexibility match the user's situation?

- **5:** Perfect match — fully remote when user is remote, plus strong flexibility signals (async culture, flexible hours, generous PTO)
- **4:** Mostly compatible — hybrid that works, or good location fit with moderate flexibility
- **3:** Requires some compromise (e.g., more in-office than ideal, or rigid schedule expectations)
- **2:** Significant friction — requires relocation or substantially changes working arrangement; or role demands on-site presence that conflicts with user preferences
- **1:** Non-starter — requires relocating to an unwanted location or fully in-office when remote is needed

**Flexibility signals to assess:** schedule autonomy, async vs. sync culture, PTO/leave policy, timezone requirements, travel expectations. These are distinct from geographic location and should be weighted in the score even when the location itself is a match.
```

---

## After All Edits — Open-Source Propagation

Per CLAUDE.md rules, propagate shared files to open-source:

1. Copy `skill-update/SKILL.md` → `open-source/skill/SKILL.md`
2. Copy `skill-update/references/mode1-offer-evaluator.md` → `open-source/skill/references/mode1-offer-evaluator.md`
3. Copy `skill-update/references/mode5-outreach.md` → `open-source/skill/references/mode5-outreach.md` (new file)
4. Copy `skill-update/references/scoring-guide.md` → `open-source/skill/references/scoring-guide.md`
5. Copy `skill-update/references/mode2-portal-scan.md` → `open-source/skill/references/mode2-portal-scan.md`
6. Copy `skill-update/references/mode9-inbox-followup.md` → `open-source/skill/references/mode9-inbox-followup.md`
7. Copy `skill-update/references/weekly-trend-report.md` → `open-source/skill/references/weekly-trend-report.md`
8. Copy `README.md` → `open-source/README.md`
9. Do NOT copy any eval or outreach files (they contain real company data).

---

## Verification

After completing all edits, verify:

1. Read `skill-update/references/mode1-offer-evaluator.md` and confirm: the output template now includes the 10-dimension scoring table, bias caveat, and career-page cross-check (signal #6 in legitimacy). The post-eval steps include a grade consistency check.
2. Read `skill-update/SKILL.md` and confirm: the frontmatter schema includes source, referral_contact, and application_method. Mode 5 points to the new reference file. Mode 3 includes the competency matrix and framework selection guidance.
3. Read 2–3 eval files from evals/ and confirm all frontmatter fields are present.
4. Read both outreach files and confirm they have the new tracking fields and follow-up schedule.
5. Read `skill-update/references/mode5-outreach.md` and confirm it exists with the full template.
6. Read `README.md` and confirm the frontmatter section includes the three new fields.
7. Read `scoring-guide.md` and confirm Dimension 8 includes flexibility signals.
8. Confirm all open-source copies are current.

Report a summary of all changes made, organized by stream, with file paths.
```

---

## Usage Notes

- **Copy everything between the ``` delimiters above** as the prompt to Sonnet.
- The prompt is designed to be self-contained — Sonnet reads the plan and all target files before editing.
- Stream D tasks D.4 and D.5 are explicitly excluded (deferred per the plan).
- The prompt specifies exact insertion points using surrounding content anchors rather than line numbers (line numbers shift as edits are made).
- Verification steps at the end ensure Sonnet checks its own work before reporting done.
