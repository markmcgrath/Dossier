# Dossier — Configuration

Per-user settings read by the `dossier` skill.

---

## Notion Tracker (Optional)

```yaml
# --- Notion Mirror (optional) ---
notion:
  enabled: false
```

---

## Optional preferences

```yaml
# default_search_location: "Remote"
# job_search_result_cap: 8
```
